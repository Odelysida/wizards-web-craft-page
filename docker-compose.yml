services:
  node:
    image: "node:current"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes: 
      - ./:/home/node/app
    ports:
      - "4173:4173"
    command: bash -c "npm install --include=dev && npm run preview"
